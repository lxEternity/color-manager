#!/system/bin/sh
# CPU/GPU 分簇限频入口（C 二进制包装）。
# conf 调用方式保持不变（$mokzdz/A/json_cpu_max_min <参数>），实际执行 bin/ 下的
# 静态 aarch64 二进制（源码见模块根 json_cpu_max_min.c，逻辑与旧 shell 版 1:1 对齐）
D=$(cd "$(dirname "$0")" && pwd)
exec "$D/../bin/json_cpu_max_min" "$@"
