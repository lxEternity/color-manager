v1.3.9.11（本仓库同步版，模块内版本号）
1. 修复 WebUI 切换调度模式后重进又变回省电模式：
   - 手动切换模式后同步写入 动态模式切换.conf 的 moren=（默认模式），
     前台监视(qtbh.sh)不再在下一次前台变化时把模式切回省电；
     应用专属规则（如 com.oplus.camera=fast）不受影响仍优先生效
   - 调度参数/调速器页进入时选中"当前运行模式"（读 cur_powermode.txt），
     不再固定停在省电模式页签
2. 修复功耗记录页实测满容量（FCC）不准：
   - 读取顺序改为 OPLUS FG 学习容量 fg_full_capacity 优先，
     charge_full / bms/charge_full 兜底（部分机型 charge_full 报设计容量导致寿命恒 100%）
   - 电压单位自适应（µV/mV/V），报 mV 的机型不再显示 0.00 V
   - FCC 读取失败时健康度如实显示 --，不再编造 ≈97% 估计值

v1.3.9.10（本仓库同步版，模块内版本号）
1. 修复功耗记录页图表空白与数据错位：
   - 修复 for-in 拼接缺空格导致的 shell 语法错误（图表整体空白）
   - 修正 CSV 列序解析：v1.3.9.7 起第 3 列为电压，旧逻辑把电压当温度画曲线、
     电量列当充放状态（充电段永远识别不到），现按列数自适应新旧两种格式
   - 历史文件名日期改由 JS 端按设备时区计算，兼容不支持 `date -d "-N days"` 的 toybox
     （此前近3天/近7天只能读到当天文件）
   - 聚合结果改走 base64 单行回传（__EOW__ 哨兵 + 原始输出兜底），防多行 stdout
     被部分管理器 exec 桥截断
2. 修复调度参数/调速器参数不读取脚本配置（显示默认值）：
   - 配置脚本内容含引号/多行，部分 KSU 兼容壳 exec 回调会截断 stdout 导致解析为空；
     loadModes/loadGov/saveGov 全部改用 readFileB64（base64 单行回传，任何桥安全）
   - 调速器页方案选择由 SOC 映射改为按调速器可用性判定（与 script/main.sh 一致：
     scx→A / walt→C / conservative→B），自定义内核机型（如 8gen2 刷风驰内核）
     不再读写错 A/B/C 目录
3. 保存保护：配置脚本读取失败时阻止保存并提示，防止误把脚本清空

v1.3.9.9（本仓库同步版，模块内版本号）
1. A 方案（一代风驰：一加13T/一加13/一加ACE6）调速器与调度参数全模式重调优：
   - 省电：conservative 93/86/步进2/采样12000µs，上限36%/下限4%，LLCC 300MHz，
     uclamp 整体下调（touch 7 / top-app 9），极限压低功耗同时保留动态升降频，日常省电流畅
   - 均衡：scx target_loads 84，上限72%/下限16%，LLCC 720MHz，opt2 28，
     触控 42 保证跟手；负载 84% 才提频运行于高效区间，120/144 帧游戏不掉帧，
     功耗对齐官方风驰调度
   - 性能：scx target_loads 74，上限94%/下限30%，LLCC 1220MHz，opt2 58；
     负载 74% 提前拉频 + 高保底频率防帧波动，和平精英 165 帧稳帧，功耗同步官方
   - 极速：walt target_loads 60，上限100%，LLCC 1800MHz，opt2 92，
     解除 walt 升频速率限制，满血释放性能
2. 修复 scx3.sh（极速模式）误写 conservative 节点：改写 walt/target_loads；
   修复 scx2.sh 路径双斜杠笔误（//proc）
3. A/json_cpu_max_min 升级为分簇限频版：
   - 兼容 2 参（全簇同上限%，出厂 conf/WebUI 格式）与 4 参（小核%/大核%/下限%/GPU%）
   - 修复 APP 保存 4 参调度配置时 min/max 语义错位的旧问题
   - 大小核按 cpuinfo_max_freq 自动分簇，GPU 经 kgsl devfreq 限频
4. APP/WebUI 默认值同步：AllConfig.defaults、GovernorActivity.DEFAULTS、
   WebUI 演示数据与新出厂参数一致

v1.3.9.8（本仓库同步版，模块内版本号）
1. 新增 C 方案（方案3）：适配无风驰(scx)调度器内核机型（骁龙 8 Gen2 / 8+ Gen1 / 8 Gen1 等）
   - 安装时检测 walt 调速器 → 自动加载 config/c.all.sh（C 方案四模式参数）
   - C 方案调速器脚本小核(cpu0-3)/大核(cpu4-7)参数独立分组
2. APP 调度参数页新增「方案 3」页签：
   - C 方案参数编辑/保存（json_cpu_max_min 保持 2 参，与出厂 c.all.sh/WebUI 格式一致）
   - 恢复默认/导入/导出支持方案3与全部方案
3. APP 调速器配置页新增「方案 3」页签：
   - C 方案小核/大核独立参数（up/down_threshold、freq_step、sampling_rate、target_loads）
   - 保存写入 C/ 目录并自动镜像备份
4. WebUI 同步 C 方案：SOC 映射、方案检测（walt→C）、调速器目录与参数滑条双分组
5. SOC 检测映射更新：8gen2 / 8+ / 8gen1 → 方案3（walt 内核）

v1.3.9.7（本仓库同步版，模块内版本号）
1. 修复功耗记录曲线被数字遮挡：主页实时曲线全局注册的数值标签插件泄漏到记录图表，
   给每个数据点都画了数值标签（密集叠在曲线上），记录图表已显式关闭
2. 功耗记录采样 30 秒 → 2 秒（数据量增大约 15 倍）：
   - 记录曲线改为 awk 设备端按时间桶聚合回传（≤2 万桶），避免大文件整体传输卡顿
   - 统计（均值/峰值/估算耗电）由 awk 按原始行精确计算，记录条数显示真实采样数
   - 新增「近1小时」时间档（2 秒采样精细度在短时段可见）
3. 修复电芯自动检测：对齐 APP PowerMonitor 判定——串联双芯（总压>5.5V）与
   并联双芯（≥2 个独立 Battery 节点），自动模式历史曲线同步修正
4. 修复双芯模式下历史功耗 ×4 异常：pwlogd 不再在写入端 ×2（CSV 恒存原始功率），
   新增第 6 列采样电压，统一由查看端按「当前电芯模式 + 每行电压」修正
5. 修复 WebUI 调速器保存无法写入（A/B 方案目录大小写）与回读误报「被外部修改」

v1.3.9.6（本仓库同步版，模块内版本号）
1. 修复实时功耗功率显示异常（充电仅 0.06W）：
   - WebUI 实时采样与 pwlogd 守护的电流/电压单位自适应（µA/mA/A、µV/mV/V），
     对齐 APP 端 PowerMonitor 校准逻辑，修复部分机型节点报 mA/mV 时功率缩小 1000 倍
2. 修复功耗记录温度显示 363℃ 异常：
   - 温度单位自适应（0.01℃/0.1℃/℃ → ℃），读取端与写入端统一校准
   - pwlogd 启动时清理旧版遗留进程（旧守护按旧公式持续写坏数据的问题）
3. 实时功耗曲线新增充电段橙色着色（与功耗记录页一致）+「充电段」图例
4. 新增手动电芯模式切换（实时功耗卡片「电芯·自动」按钮，循环 自动/单电芯/双电芯）：
   - 持久化到 /data/adb/colorFC_store/cellmode，WebUI 重开/重启/重刷均保持
   - 双芯模式且节点只报单芯数据时功率×2（对齐 APP applyCellMode），实时与记录曲线同口径

v1.3.9.10
1. 修复功耗记录曲线温度显示为 300 多度的问题：
   - pwlogd 写入端归一：battery/temp（0.1℃ 内核单位）写入 CSV 前转为 ℃
   - 读取端兼容：旧版已积累的 0.1℃ 历史数据自动按 >100 除以 10 修正
2. 曲线图触控手感优化（主页实时曲线 + 功耗记录曲线）：
   - 横向手势图表响应（tooltip 贴近触点跟随），竖向手势正常滚动页面不卡图表
   - 触摸命中容差提升（pointHitRadius 18），tooltip 竖线全宽吸附（axis:'x'）
   - 主页曲线新数据点平滑滑入（260ms 缓动），不再生硬跳变

v1.3.9.9
1. 主页改版：
   - 形态切换改为紧凑横条（形态名+提示+小按钮），移除大卡片
   - 实时功耗曲线上移为主页第一卡片
   - 移除系统状态卡片（当前调速器仍可在调速器页查看）

v1.3.9.8
1. 新增「功耗记录」页（WebUI 底部导航第 4 项）：
   - 功耗记录曲线：模块后台守护 pwlogd.sh 每 30 秒采样电池功率/温度/电量/充放状态写入
     /data/adb/colorFC_store/pwlog（两形态常驻、保留 7 天、形态切换/重刷不丢失），
     曲线放电段绿色/充电段橙色/温度红线，支持近 24 小时/3 天/7 天范围，>600 点自动降采样
   - 统计：记录条数/平均放电功率/平均充电功率/最高温度/估算耗电/覆盖时长
   - 电芯检测：设计容量/实测满容(FCC)/电池寿命(FCC÷设计容量)/循环次数/电芯电压/电池技术，
     OPLUS 与标准电池节点多级回退，支持重新检测
2. APP 主页新增实时功耗曲线（v1.3.9）：双 Y 轴功率/温度曲线，放电绿/充电橙分色、
   末点胶囊数值标签，30 点滚动，随实时功耗采样更新
3. 模块内置管理器 APK 更新为 v1.3.9（含曲线功能）

v1.3.9.7
1. 主页新增实时功耗曲线（图表引擎与配色照搬 opbatt 电池工具包，Chart.js + datalabels）：
   - 功率（W）/电池温度（℃）双 Y 轴曲线，滚动记录最近 30 条采样（2 秒/条），localStorage 持久化
   - 顶部实时功率 / 电池温度 / 充放状态三数据格，充电中·放电中·已接电状态自动识别
   - 曲线末端胶囊数值标签、深色圆角悬浮提示，离开主页自动停止采样
2. 调速器方案选择改为 SOC 识别（与 Color 管理器 APP 完全一致）：
   - 13 项 SOC 映射表（sm8850/8845/8875/8950/8975 → 方案2，其余骁龙8系与天玑旗舰 → 方案1，未知默认方案1）
   - getprop ro.board.platform → ro.soc.model → ro.mediatek.platform → ro.hardware 多级回退
   - 调速器页显示"当前SOC：xxx → 加载方案 x"，当前调速器按方案显示 scx/conservative（不再读 sysfs，
     避免部分设备 scaling_governor 读值异常导致显示不正常）
3. 移除调度参数页与调速器页的"写入 xxx 路径"提示

v1.3.9.6
1. WebUI 修复：主页"大核最高频"改为遍历全部 cpufreq policy 取最大值（不再依赖 policy7，适配无 policy7 的 SoC）
2. WebUI 文案简化：形态卡片显示"Color调度"/"自适应限频"，提示改为
   "当前模式为Color调度模式，点击切换到自适应限频"（自适应形态反之）
3. 调速器页当前调速器读取改为 policy0（与主页一致，避免大核 policy 缺失时显示空）

v1.3.9.5
1. 新增 KernelSU/APatch 标准模块 WebUI（webroot/index.html）：管理器内直接打开，无需 Termux/Python
   - 主页：模块形态展示 + 一键切换、模式快切、系统状态（手动刷新，非监视器）
   - 调度参数：四模式 opt2/CPU上下限/LLCC/uclamp/WALT 滑条编辑，写入当前活动方案 conf，当前模式实时应用
   - 调速器：四模式调速器切换与参数编辑，写入 A/B 方案脚本
2. 自适应限频（Ktwo powerd_freqcap）合并为模块内置形态，WebUI 一键切换：
   - 切换到自适应：完全卸载 Color 调度（模块文件 + /data/powercfg 运行时接口）并备份到 /data/adb/colorFC_store，
     全新释放自适应限频文件到 ColorFC 目录，模块 ID/目录同步 colorFC，自动重启管理器刷新模块列表
   - 切换回调度：自适应守护干净退出并恢复频率快照，调度文件重新释放，保留期间对 games.txt/powerd.conf 的修改
   - 注意：重刷/更新模块安装包会恢复为调度形态；/data/adb/colorFC_store 存有形态备份请勿手动清理
3. 移除旧版 Python 本地服务器 WebUI（webui/server.py），service.sh 相应清理
